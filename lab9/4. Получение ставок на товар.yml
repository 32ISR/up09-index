info:
  name: 4. Получение ставок на товар
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/items/:id/bids"
  params:
    - name: id
      value: "1772524794267"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
