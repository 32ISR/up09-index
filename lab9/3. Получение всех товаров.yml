info:
  name: 3. Получение всех товаров
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/items"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
