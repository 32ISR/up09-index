info:
  name: 5. Статистика платформы
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/stats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
