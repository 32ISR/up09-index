info:
  name: 3. Удаление товара
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base_url}}/api/items/:id"
  params:
    - name: id
      value: "1773297443872"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
