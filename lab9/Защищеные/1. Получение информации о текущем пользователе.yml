info:
  name: 1. Получение информации о текущем пользователе
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/auth/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
